<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meetback.dev.repository.MeetingCandidateMapper">

    <insert id="insert"
            parameterType="com.meetback.dev.domain.MeetingCandidate"
            useGeneratedKeys="true"
            keyProperty="candidateId">

        INSERT INTO meeting_candidates (
        meeting_id,
        proposer_participant_id,
        place_name,
        address,
        latitude,
        longitude,
        is_active
        )
        VALUES (
        #{meetingId},
        #{proposerParticipantId},
        #{placeName},
        #{address},
        #{latitude},
        #{longitude},
        TRUE
        )

    </insert>

    <select id="findByMeetingId"
            parameterType="long"
            resultType="com.meetback.dev.domain.MeetingCandidate">
        SELECT
        candidate_id,
        meeting_id,
        proposer_participant_id,
        place_name,
        address,
        latitude,
        longitude,
        is_active,
        created_at,
        updated_at
        FROM meeting_candidates
        WHERE meeting_id = #{meetingId}
        AND is_active = TRUE
        ORDER BY candidate_id
    </select>

    <select id="findById"
            parameterType="long"
            resultType="com.meetback.dev.domain.MeetingCandidate">
        SELECT
        candidate_id,
        meeting_id,
        proposer_participant_id,
        place_name,
        address,
        latitude,
        longitude,
        is_active,
        created_at,
        updated_at
        FROM meeting_candidates
        WHERE candidate_id = #{candidateId}
    </select>
</mapper>